kind: will-be-ignored
metadata:
  name: will-be-ignored
spec:
  values:
    # https://github.com/VictoriaMetrics/helm-charts/tree/master/charts/victoria-metrics-k8s-stack
    grafana:
      ingress:
        enabled: true
        ingressClassName: kong
        hosts:
          - test1-grafana.writefor.fun
    vmsingle:
      ingress:
        enabled: true
        ingressClassName: kong
        hosts:
          - test1-vm.writefor.fun
